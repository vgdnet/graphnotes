---
description: Technical architecture goes through MASTER_CONTEXT; do not invent a second stack
alwaysApply: true
---

# Technical canon (chief technical agent)

Technical architecture, runtime, stack, and technical TZ are one file:
`docs/context/MASTER_CONTEXT.md` plus last accepted `PRODUCT_SPEC` **3.40**
(owner 2026-09-21; ingest remains 3.37 plugin / no external git host).

- Read **SPEC + MASTER**, not a stack of ADR. Living vs superseded:
  MASTER §0 and `docs/decisions/README.md`. Do not treat superseded
  files as ingest/RBAC/circulation canon.
- Do not invent a second stack, second ingest, or a second Markdown canon.
- Do not amend an ADR or change runtime without matching `MASTER_CONTEXT.md`.
- Technical questions go through the technical agent / `MASTER_CONTEXT.md`.
- Product editor writes product TZ only (`docs/product/**`). Code writers
  implement the same two files. Leftover runtime is unfinished code, not a
  second spec.

Knowledge has **no git**. Ingest is Obsidian → plugin → GraphNotes stores.
GitHub is ADR-006 source-code delivery only (repo **private**;
`nord → GitHub → rhizome-test`). Living GitHub product name is
`https://github.com/vgdnet/rhizome` (not ingest; not host `rhizome`).
Editor text-diff is native C++ wikidiff2 (PHP is not the engine).
Live plugin: `obsidian-card-merge/`.
Grants: `access_grants` `(user, path|tag|prefix)`; empty ≠ whole queue.
